with Halls;

package Interruptor is
   procedure Print_Interrupt (ID : in Halls.Hall_ID);
   -- Simple procedure that will print to the screen. Used for callback
   --   in Halls_Test

end Interruptor;
